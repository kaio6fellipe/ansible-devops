---
plugin: aws_ec2

aws_profile: default

regions:
  - us-east-1

filters:
  tag:env: prod

keyed_groups:
  - key: tags.env
    separator: ""
  - key: tags.type
    separator: ""
    parent_group: "{{ tags.env }}"

hostnames:
  - private-ip-address
  - ip-address
  - dns-name
  - tag:Name
